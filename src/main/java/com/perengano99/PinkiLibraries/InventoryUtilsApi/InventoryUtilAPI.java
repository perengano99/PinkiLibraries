package com.perengano99.PinkiLibraries.InventoryUtilsApi;

import org.bukkit.Material;
import org.bukkit.inventory.Inventory;
import org.bukkit.inventory.ItemStack;

public class InventoryUtilAPI {
    
    public void addItem(Inventory inventory, ItemStack item) {
//	if (item.getType() == Material.PLAYER_HEAD) {
//	    if (inv.contains(item)) {
//		for (int v = 0; v < 9; v++) {
//		    if (SlimefunManager.isItemSimiliar(inv.getContents()[v], item, true, DataType.NEVER)
//			    && inv.getContents()[v].getAmount() < 64) {
//			inv.getContents()[v].setAmount(inv.getContents()[v].getAmount() + item.getAmount());
//			break;
//		    }
//		}
//	    } else
//		inv.addItem(item);
//	} else
//	inv.addItem(item);
    }
    
}
